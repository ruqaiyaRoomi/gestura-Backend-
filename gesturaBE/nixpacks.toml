[phases.setup]

nixPkgs = ["python311", "nodejs", "python311Packages.pip", "stdenv.cc.cc.lib"]

[phases.install]
cmds = ["npm ci", "python3 -m venv .venv", 
".venv/bin/pip install -r requirements.txt" ]