[phases.setup]

nixPkgs = ["python311", "nodejs", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci", "python3 -m venv .venv", ". .venv/bin/activate", "pip install --upgrade pip", "pip install -r requirements.txt" ]